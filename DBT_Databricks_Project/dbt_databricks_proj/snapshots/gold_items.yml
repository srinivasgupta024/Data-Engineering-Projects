snapshots:
    - name: gold_items_snapshot
      relation: ref('source_gold_items')
      config:
        schema: gold
        database: '{{ target.catalog }}'
        unique_key: id
        strategy: timestamp
        updated_at: updatedDate
        dbt_valid_to_current: "to_date('9999-12-31')"